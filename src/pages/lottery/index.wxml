
<view class="wrap {{user$.IsMember && 'g-vip-page'}}">
    <image
        mode="widthFix"
        src="{{src$.bg}}"
    ></image>
    <button
        class="g-button button"
        bindtap="handleJump"
    >立即打卡</button>
    <view class="link"><text>查看规则</text></view>
</view>
