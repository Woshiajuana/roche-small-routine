<view class="flex-wrap">
    <view class="header">
        <image src="{{src$.icon}}"></image>
        <text>{{blueTooth ? '已发现'+ blueTooth.length +'个设备，点击绑定' : '寻找设备中，请耐心等待...'}}</text>
    </view>
    <view class="inner">
        <view class="before" hidden="{{blueTooth !== '' && blueTooth !== null}}">
            <image src="{{src$.nIcon}}"></image>
            <text>请确保待绑定设备在手机旁边</text>
            <text>并且已经开机</text>
        </view>
        <view class="after" hidden="{{blueTooth === null || blueTooth === ''}}">
            <view class="device-item"
                  wx:for="{{blueTooth}}"
                  wx:for-item="item"
                  wx:key="item">
                <image mode="widthFix" class="device-icon" src="{{src$.result}}"></image>
                <view class="device-item-con">
                    <text>{{item.name}}</text>
                    <text class="no-text">{{item.deviceId}}</text>
                </view>
                <view
                    class="device-button"
                    bindtap="handlePairRoche"
                    data-device-id="{{item.deviceId}}">
                    <text>连接</text>
                </view>
            </view>
        </view>
        <view class="form-button"
              hidden="{{blueTooth === null || blueTooth === ''}}"
              data-value="{{true}}"
              bindtap="searchRoche">
            <text class="form-button-text">重新搜索</text>
        </view>
    </view>
    <view hidden="{{blueTooth}}">
        <view class="tel">
            <text>如有问题，您可以拨打罗氏客户关爱热线</text>
            <text class="tel-text">800 820 2777（固话）</text>
            <text class="tel-text">400 820 2777（手机）</text>
            <text>工作时间每天8：00-20：00（法定节假日除外）</text>
        </view>
        <view class="prompt">
            <text>部分安卓机型，无法正常使用蓝牙连接</text>
            <view class="link"
                  data-url="record_index"
                  bindtap="handleJump">
                <text>点击前往手动记录</text>
            </view>
        </view>
    </view>
    <view class="flex-1"></view>
    <import src="../../../templates/bluetooth-explain/index.wxml"/>
    <template is="bluetoothExplain"></template>
</view>
